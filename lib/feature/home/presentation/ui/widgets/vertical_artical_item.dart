import 'package:flutter/material.dart';

class VerticalArticalItem extends StatelessWidget {
  const VerticalArticalItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}